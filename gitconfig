[user]
	name = Cédric Cabessa
	email = ced@ryick.net
#[http]
#	proxy = localhost:3128
[color]
	ui = auto
[sendemail]
        smtpencryption = tls
        smtpserver = ced.ryick.net
        smtpuser = xxx
        smtpserverport = 25
[help]
	autocorrect = 20
[alias]
	subup = submodule update --init --recursive
        propre = "!f() { git clean -xfd || sudo git clean -xfd; }; f"
        which = !git branch -a | grep -i
        lucky = "!f() { branch=$(git branch | grep -i -m1 $1) && git checkout $(git branch | grep -i -m1 $1) ; }; f"
        wdiff = diff --word-diff=color
        bourre = push --force-with-lease
        stashi = stash --keep-index
[status]
	submoduleSummary = true
[rebase]
	autostash = true
[stash]
	showPatch = true
[commit]
	verbose = true
[diff]
	indentHeuristic = true
	colorMoved = default
